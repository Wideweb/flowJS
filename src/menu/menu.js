export default class Menu {
    constructor(items) {
        this.items = items;
    }

    load(container) {
        for (let i = 0; i < this.items.length; i++) {

        }
    }

    unload(container) {
    }

    update(gameTime) {
    }
}