import { IAppTime } from "../app";
import BasePlayerController from "./base-player-controller";

export default class AIPlayerController extends BasePlayerController {

	load(): void { }

	unload(): void { }

	update(gameTime: IAppTime): void { }
}